/**
 * Horizontal store nav removed — Store Managers use the left sidebar instead.
 * Kept as a no-op so existing page imports do not break.
 */
export default function StoreManagerNav() {
  return null;
}
